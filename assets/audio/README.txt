Drop your background music file here, named exactly:

    ambient.mp3

The site's "sound // off" button (bottom-left) plays this file on click,
loops it, and starts at 45% volume.

To use a different filename or format (.m4a/.ogg), update the <source>
inside index.html:  <audio id="bgm"> ... <source src="assets/audio/ambient.mp3" ...>

Tip: use a royalty-free / licensed track. Keep it small (a 2-4 MB mp3 is plenty).
